TODO: Cache control headers https://www.php.net/manual/en/function.readfile.php https://www.electrictoolbox.com/php-caching-headers/

TODO: Use caching for blog posts and blog list

TODO: Use caching on pages (using page ID from abstract parent class)

TODO: skali-like in-page editable areas (w/ markdown)

TODO: Didn't receive email on subscription

TODO: Don't automatically reset password (in case someone else does it to annoy people), just send a link they can ignore

TODO: Admin, make it possible to configure a list of models to be editable in admin panel (Django-like). Reference the model, && database table name


TODO: setAttribute('class', ''); -> addClass('');

<?php
	if ($this->m_app->getUser()->isLoggedIn()
	    && $this->m_app->getMemberManager()->memberIs('editor')) {

		$template->linkFiles([
			'js/lib/Goji/InPageContentEdit-19.11.14.class.js'
		]);
	?>
		<script>
			(function () {

				document.querySelectorAll('.in-page-content-edit').forEach(el => new InPageContentEdit(el));

			})();
		</script>
	<?php
	}
?>

BEGIN TRANSACTION;

----
-- Table structure for g_in_page_editable_content
----
CREATE TABLE 'g_editable_content' ('id' INTEGER PRIMARY KEY AUTOINCREMENT NOT NULL, 'content_id' TEXT, 'page_id' TEXT, 'locale' TEXT, 'content' TEXT, 'last_edit_date' DATETIME, 'last_edit_by' INTEGER);
COMMIT;

