/*
 * Configuration file for \Goji\Core\SimpleCache (or any other custom caching engine)
 *
 * Missing info will be ignored.
 */

{
	/*
	 * Activate or deactivate Goji's internal cache (SimpleCache).
	 *
	 * /!\ 'false' should be for DEBUG ONLY. Goji makes use of caching in its internal engine.
	 * So, unless you rewrite those library elements, you should leave it on (even if you use
	 * another caching system in your own code).
	 */
	use_cache: true
}
